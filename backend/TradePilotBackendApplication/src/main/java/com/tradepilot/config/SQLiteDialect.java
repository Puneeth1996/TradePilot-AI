package com.tradepilot.config;

/**
 * Placeholder dialect class. The application now uses Hibernate community dialect
 * configured in `application.properties`.
 */
public final class SQLiteDialect {
}
